# IRIS emitters

Generated by Truth Compiler. Copy Semgrep rules into the repo Semgrep config;
copy path instructions into the org or repo CodeRabbit config with inheritance.
Do not use these files as a second AI reviewer.

Live truths: 16
Semgrep rules (whole checkout, semgrep.yml): 1
Semgrep rules (diff scan only, semgrep-diff-scan.yml): 1
CodeRabbit path instructions: 1
Visible gaps: 5

## Scope matters

`semgrep-diff-scan.yml` rules come from truths that only adjudicate added
lines. They must be run with `--baseline-commit`; a repository-wide run
reports pre-existing code the truth does not claim.
