id: IRIS-TRUTH-0019
tenant: iris
title: Do not use leading-wildcard LIKE on Care integration metadata
statement: >
  Queries must not use LIKE '%…%' against int_meta or other large Care tables.
  Leading wildcards cannot use indexes and were a blast-radius amplifier.
status: live
executor:
  kind: semgrep
  blocking: true
  emit: semgrep
  languages:
    - typescript
    - javascript
    - java
  mode: both
  forbidden_line_patterns:
    - "LIKE\\s+['\"]%"
    - "int_meta LIKE '%"
applies_to:
  repositories:
    - ignitetech-group/iris-api
  paths:
    - src/**/*.ts
  excluded_paths:
    - "**/*.test.ts"
    - "**/__tests__/**"
evidence:
  - type: rca
    key: CARESMM-24342
    url: https://ignitetechpm.atlassian.net/wiki/spaces/IRIS/pages/1497301004
    note: pod5 LIKE on a 114GB table
failure_mechanism: >
  A leading-wildcard LIKE on int_meta scanned a huge table while an in-memory
  watermark already caused a republish storm.
required_guards:
  - Use JSON_EXTRACT or parameterized prefix matches, never LIKE '%…%'
governance:
  owner: care-compatibility-maintainers
  approved_at: 2026-09-01
  review_after: 2027-03-01
  version: 1
