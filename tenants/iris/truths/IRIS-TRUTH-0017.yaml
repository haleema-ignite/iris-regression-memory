id: IRIS-TRUTH-0017
tenant: iris
title: Publisher AI customer actions remain reachable
statement: >
  Publisher AI actions that agents used from the calendar and composer must
  remain reachable after calendar and assets refactors. This stays a gap until
  a stable source selector is named the same way Generate Campaign is.
status: gap
# No files are named on purpose. The gap is precisely that no stable selector
# exists for these actions yet, so there is nothing to assert about any file.
# Naming one without an assertion would look like coverage.
executor:
  kind: product
  blocking: false
  emit: none
applies_to:
  repositories:
    - ignitetech-group/iris-web
  paths:
    - src/features/publishing/**
  always_on: true
evidence:
  - type: slack
    note: Publisher AI broken after calendar and assets fixes
failure_mechanism: >
  Shared-shell refactors dropped a customer action that had no named presence check.
required_guards:
  - Name the Publisher AI entry points and convert this gap to a live product truth
governance:
  owner: publisher-maintainers
  version: 1
