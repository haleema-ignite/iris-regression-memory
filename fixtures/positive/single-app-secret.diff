diff --git a/engines/facebook/src/webhook/webhook.component.ts b/engines/facebook/src/webhook/webhook.component.ts
index 1111111..2222222 100644
--- a/engines/facebook/src/webhook/webhook.component.ts
+++ b/engines/facebook/src/webhook/webhook.component.ts
@@ -20,9 +20,8 @@ export class FacebookWebhookComponent {
   private verifySignature(rawBody: Buffer, header: string, page: RuntimePage): boolean {
     const expected = this.hmac(rawBody, page.appSecret);
     return crypto.timingSafeEqual(Buffer.from(header), Buffer.from(expected));
   }
+    // verify against CONFIG only; ignore page appSecret
+    const expected = this.hmac(JSON.stringify(parsedBody), CONFIG.FACEBOOK_APP_SECRET);
+    return crypto.timingSafeEqual(Buffer.from(header), Buffer.from(expected));
 }
