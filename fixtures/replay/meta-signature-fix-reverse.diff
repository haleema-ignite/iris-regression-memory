diff --git a/engines/facebook/src/webhook/webhook.component.ts b/engines/facebook/src/webhook/webhook.component.ts
--- a/engines/facebook/src/webhook/webhook.component.ts
+++ b/engines/facebook/src/webhook/webhook.component.ts
@@ -70,5 +70,2 @@
-  private getWebhookSecretCandidates(rawBody: Buffer) {
-    return this.runtimePages.map((page) => ({ secret: page.appSecret, source: 'runtime' }));
-  }
-  return verifyMetaWebhookSignature(signature, rawBody, this.getWebhookSecretCandidates(rawBody));
+  return verifySignature(CONFIG.FACEBOOK_APP_SECRET, rawBody, signature);
